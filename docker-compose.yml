services:
  price-radar:
    build: .
    container_name: price-radar
    ports:
      - "8501:8501"
    env_file:
      - .env
    volumes:
      - ./data:/app/data
      - ./logs:/app/logs
    command: streamlit run dashboard/app.py --server.address=0.0.0.0 --server.port=8501
